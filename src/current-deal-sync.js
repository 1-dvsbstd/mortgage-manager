(() => {
  // V0.9.5 compatibility stub.
  // Current-deal synchronisation now lives in mortgage-history.js and MortgageStore.
  // Kept temporarily so older cached index files that still reference this script
  // do not fail while the V0.9 migration is being consolidated.
})();
