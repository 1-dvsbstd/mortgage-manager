(() => {
  // Offline V1 is glance-first: main dashboard cards no longer open as full-screen
  // expandable cards. Keeping this file as an intentional no-op preserves the
  // existing asset graph while preventing legacy click/touch handlers, Close
  // bars, backdrops and layout jitter from initialising.
})();
